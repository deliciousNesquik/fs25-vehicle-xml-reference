# Farming Simulator 2025
## Блок `<ИМЯ_БЛОКА>` в XML техники/оборудования

Одна строка назначения блока.

Источник схемы: `<Lua-функция / .xsd>` (`FS25`). Все атрибуты и значения по умолчанию — из исходного кода.

---

## 1. Что это

2–4 предложения: что описывает блок и когда применяется. Термин[^term] с короткой сноской.

---

## 2. Атрибуты

| Атрибут | Тип | По умолчанию | Описание |
|---|---|---|---|
| `attr` | TYPE | `default` | Описание. |

Пример:

```xml
<block attr="value"/>
```

---

## 3. Под-элементы

(если есть)

---

## 4. Примеры XML

Минимум один пример на каждую группу параметров.

```xml
<block>
    ...
</block>
```

---

## 5. Устройство в GIANTS Editor

(если релевантно: какие ноды, типы, соглашения имён)

---

## 6. Типичные ошибки

- Ошибка — причина — решение.

---

## Глоссарий

[^term]: Определение термина. <https://ссылка>
